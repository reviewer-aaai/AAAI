# Reproduction environment - SU(2) x Sim(2) on MNIST
#
# Python 3.12 required.
#     torch-harmonics publishes wheels for CPython 3.10-3.12 only, and the
#     0.9.x releases have no source distribution. On Python 3.13, pip silently
#     falls back to 0.8.0, whose spherical transforms behave differently.
#
#     On Python 3.13, build from source instead:
#       pip install --no-build-isolation \
#           "git+https://github.com/NVIDIA/torch-harmonics.git@v0.9.1"

torch==<x.y.z>
torchvision==<x.y.z>
torch-harmonics==0.9.1

numpy
pandas
matplotlib
tqdm

# -- Implementation note ------------------------------------------------------
# SpectralPoolS2 passes mmax=out_lmax explicitly to InverseRealSHT rather than
# relying on the library default, which is nlon//2+1 in recent releases. The
# model is therefore independent of the torch-harmonics version; the pin above
# only serves strict numerical reproducibility.
